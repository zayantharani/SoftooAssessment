import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        marginTop: 50,
        marginBottom: 20,
        alignItems: 'center'
    },
    welcome: {
        fontSize: 20,
        color: '#000'
    },
    username: {
        fontSize: 18,
        color: 'gray'
    }
});
